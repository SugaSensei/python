import math

print("this program is for calculating the area of rectangle")
print("-----------------------------------------------------")
length = float(input("enter the length "))
width = float(input("enter the width "))
area = width * length
print("-----------------------------------------------------")
